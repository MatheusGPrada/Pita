import styled from 'styled-components/native'

export const Image = styled.Image`
    justify-content: center;
    align-items: center;
    width: ${({ size }) => size};
    height: ${({ size }) => size};
`
