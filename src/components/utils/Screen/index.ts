import styled from 'styled-components/native';

export const Content = styled.View`
    flex: 1;
`;
//padding: ${({ theme }) => `${theme.marginsPx.m16px} ${theme.marginsPx.m16px} ${theme.marginsPx.m24px}`};
